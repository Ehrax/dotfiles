# Tooling

Use the `fff` MCP tools for file and content search whenever available.

# Model routing

Before selecting or delegating models, read `~/Projects/ehrax.dev/Dotfiles/configs/agents/model-table.md`.

# Atlas

- Atlas lives at `~/Documents/99_Vaults/Atlas`. Read `index.md` and `SCHEMA.md` before using it.
- Read anything. Write only to `00_inbox/` and `20_raw/`.
- Never write to `10_wiki/`; pages enter it only through Alex's gate.
- `20_raw/` is append-only; corrections create a new version.
